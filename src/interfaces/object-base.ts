export interface ObjectBaseInterface {

    /**
     * Virtual id is used for identification of object or for debugging proposes.
     */
    virtualId: number;

    /**
     *
     */
    getName(): string;
}
